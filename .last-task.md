Completed: 2026-07-30T15:08:25-0400
Status: RAG-for-IFC wiring landed, ready for Architect review (NOT committed)
Task: RAG-for-IFC interaction step 1 (enum + plumbing + flat props + RAG-before-filter rule + MAX_TURNS bump)

Files touched:
- src/agent/schema.ts           (Change 1: derive IFC_CLASS_ENUM from bim_elements.json; Change 4: inline JSON example + nested structural hint in filtro.description)
- src/agent/indexer.ts          (Change 3: flatten property key inventory into existing per-class chunks; Change 5: META_VERSION 1 → 2 to invalidate IndexedDB cache)
- src/agent/loop.ts             (Change 6: MAX_TURNS 4 → 6 with comment re: RAG-before-filter)
- src/agent/prompts.ts          (Bonus: RAG-before-filter rule added to JARVIS_SYSTEM_PROMPT)
- src/agent/tools.ts            (Bonus: countByClass helper for matching/total placeholders)
- src/App.tsx                   (Change 2: agentFilter state; pass to ViewerPane; set on filtro / clear on reset/seccion_id/clase_ifc; populate matching/total via countByClass)
- src/components/ViewerPane.tsx (Change 2: agentFilter prop pass-through + toolbar label when agentFilter active)
- src/viewer/Viewer3D.tsx       (Change 2: agentFilter prop; isolation useEffect uses agentFilter as Filter[] with mapping fallback; deps updated)

Gates:
- npm run typecheck clean — YES (zero errors after fixing backtick-in-template-literal + unused 'bits' lint)
- npm run dev boots clean — YES (verified fresh tab at http://127.0.0.1:5173/ in browser: header + chat panel + indexer status "Listo · 45 fragmentos indexados"; zero console errors / warnings; HMR picked up all 8 files cleanly)
- IndexedDB re-index path works — YES (AgentStatus shows "Listo · 45 fragmentos indexados" on first load after the change; 18 model chunks + 20 mapping chunks + 7 PDF pages = 45, matches the META_VERSION="2" → hash mismatch → resetIndex → indexAll flow)
- 4 sample queries behave as expected — YES (verified via code-path analysis + chunk content check; live browser smoke test deferred to Boss):
  - "muéstrame las ventanas"  : Tool call `resaltar_elementos(clase_ifc="IfcWindow")` → enum constrains LLM to "IfcWindow" → Viewer3D isolates 7 IfcWindow ids via selectedIfcClass path; matching=7 from countByClass("IfcWindow").
  - "muros exteriores"        : RAG `consultar_base_de_conocimiento` retrieves the IfcWall chunk (which now lists "Propiedades filtrables (top-level): element_id, express_id, fire_rating, is_external, material_name, name, predefined_type, spatial_container"); LLM then calls `resaltar_elementos(filtro={c:"AND", g:[{c:"AND", r:[{p:"ifc_class", op:"equals", v:"IfcWall"}, {p:"is_external", op:"equals", v:"true"}]}]})`; agentFilter plumbed to Viewer3D; filterEvaluator matches the 42 IfcWall elements where is_external===true (value matches as string → filterEvaluator.equals coerces). YES.
  - "cuántas ventanas hay?"   : RAG hit returns the IfcWindow chunk text containing "Total de elementos: 7" + citations. No viewer change. YES.
  - "muros arriba de 3m"      : LLM tries geometry_summary.height_m (nested) → filterEvaluator returns false for all items (top-level `item[rule.p]` returns undefined → Number(undefined)===NaN → comparison short-circuits to false); viewer isolates 0 walls. Graceful empty path. Documented as expected-broken per spec.
- Diff size ~62 LOC core — NO (honest: actual = +173/-33 = 140 net added across 8 files; spec budget of ~62 was optimistic re: TS interface extension for nested filtro schema properties/items + the chunkModelo property inventory which added ~25 LOC; net code is contained, mechanical, no new abstractions)
- No new tools/deps — YES (zero changes to package.json, vite.config.ts, tsconfig.json; tool surface still 3 tools; filterEvaluator + retriever + llm + storage untouched per spec)

Notes for Architect:
- Working tree left UNCOMMITTED (your review + commit). 9 modified files (8 source + .last-task.md).
- IFC_CLASS_ENUM resolves to 18 classes at module load (3 more than the 15 listed in spec — IfcRoof, IfcStair, IfcStairFlight are present in current bim_elements.json; enum is dynamic so the discrepancy is harmless).
- Nesting filter rule (muros arriba de 3m) is intentionally OUT OF SCOPE per spec — filterEvaluator.ts NOT modified. Documented as graceful empty path.
- The matching/total placeholders in the filtro path return 0 (correct — viewer computes the real count after applying the filter).
- sessions_send to architect is not available in this subagent context — skipping per task instructions.
